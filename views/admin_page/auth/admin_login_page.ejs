<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Admin Login - BookMyTicket</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" />

  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
   <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

  <!-- Custom CSS with animation -->
  <style>
    body {
      background: linear-gradient(to right, #81cfc8, #dbefff);
      min-height: 100vh;
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow-x: hidden;
    }

    .login-card {
      background-color: white;
      border-radius: 20px;
      overflow: hidden;
      animation: slideIn 1s ease forwards;
      opacity: 0;
    }

    @keyframes slideIn {
      from {
        transform: translateY(50px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }

    .login-img {
      max-width: 100%;
      height: auto;
      animation: fadeIn 2s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: scale(0.95);
      }
      to {
        opacity: 1;
        transform: scale(1);
      }
    }

    .form-label {
      font-weight: bold;
    }

    .input-group-text {
      background-color: #81cfc8;
      border: none;
    }

    .form-control:focus {
      box-shadow: 0 0 10px rgba(129, 207, 200, 0.5);
    }

    .btn-login {
      background-color: #16a085;
      color: white;
      width: 100%;
      transition: all 0.3s ease-in-out;
    }

    .btn-login:hover {
      background-color: #138d75;
      transform: scale(1.05);
    }

    .alert {
      animation: slideDown 0.6s ease-out;
    }

    @keyframes slideDown {
      from {
        transform: translateY(-20px);
        opacity: 0;
      }
      to {
        transform: translateY(0);
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <section class="container">
    <% if (success_mssg[0]) { %>
      <h5 class="alert alert-success text-center mt-3"><%= success_mssg %></h5>
    <% } %>
    <% if (error_mssg[0]) { %>
      <h5 class="alert alert-danger text-center mt-3"><%= error_mssg %></h5>
    <% } %>

    <div class="login-card row g-0 shadow-lg">
      <div class="col-md-6 d-flex align-items-center justify-content-center bg-light">
        <img src="/admin-login.jpg" alt="Admin login" class="login-img p-4" />
      </div>
      <div class="col-md-6 p-5">
        <h3 class="text-center mb-4">Admin Login</h3>
        <form action="/admin/login-data" method="POST">
          <div class="mb-3">
            <label for="email" class="form-label">Email Address</label>
            <div class="input-group">
              <span class="input-group-text">
                <i class="bi bi-envelope-fill"></i>
              </span>
              <input type="email" class="form-control" name="email" id="email" placeholder="Enter your email" required />
            </div>
          </div>

          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <div class="input-group">
              <span class="input-group-text">
                <i class="bi bi-lock-fill"></i>
              </span>
              <input type="password" class="form-control" name="password" id="password" placeholder="Enter your password" required />
            </div>
          </div>

          <button type="submit" class="btn btn-login p-3 mt-4">Login</button>
        </form>
      </div>
    </div>
  </section>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
